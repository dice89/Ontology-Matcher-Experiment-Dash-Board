'use strict';

describe('myApp.experimentsTable module', function() {

  beforeEach(module('myApp.experimentsTable'));

  describe('experimentsTable controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('tableCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});